<%= turbo_page_requires_reload %>
<%= content_for :title, "Dòng tiền theo ngày" %>
<%= content_for :subtitle, "Báo cáo dòng tiền lưu chuyển theo ngày" %>

<%= content_for :page_header_actions do %>
  <%= render Views::Reports::DailyCashFlows::FilterForm.new(form: @form) %>
<% end %>

<%= render Views::Reports::DailyCashFlows::Index.new(daily_flows: @daily_flows, summary: @summary) %>
